# WinDBG_Install

Currently, `WinDBG` is release as Microsoft store APP. But, sometimes we still need the completely version for some tools, such as `adplus`.

Here, I backup its installer from Windows SDK. Please feel free to download and use.